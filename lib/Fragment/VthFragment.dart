import 'package:flutter/cupertino.dart';

class VthFragment extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text("This is the fifth fragment"),
      ),
    );
  }
}
