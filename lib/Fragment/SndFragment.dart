import 'package:flutter/cupertino.dart';

class SndFragment extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text("This is the second fragment"),
      ),
    );
  }
}
