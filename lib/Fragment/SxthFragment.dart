import 'package:flutter/cupertino.dart';

class SxthFragment extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text("This is the sixth fragment"),
      ),
    );
  }
}
