import 'package:flutter/cupertino.dart';

class EgthFragment extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text("This is the 8th fragment"),
      ),
    );
  }
}
